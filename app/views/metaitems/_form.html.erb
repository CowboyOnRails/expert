<%= form_for [@resource, @metaitem], :url => polymorphic_path([@resource, @metaitem]), :method => :put do |f| %>
  <% if @metaitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metaitem.errors.count, "error") %> prohibited this metaitem from being saved:</h2>

      <ul>
      <% @metaitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :keywords %><br />
    <%= f.text_area :keywords %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

  <%= render "/shared/tinymce"%>